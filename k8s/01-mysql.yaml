apiVersion: v1
kind: Secret
metadata:
  name: poll-mysql-secret
type: Opaque
stringData:
  root-password: rootpassword
---
apiVersion: v1
kind: Service
metadata:
  name: poll-mysql
spec:
  clusterIP: None
  selector:
    app: poll-mysql
  ports:
    - port: 3306
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: poll-mysql
spec:
  serviceName: poll-mysql
  replicas: 1
  selector:
    matchLabels:
      app: poll-mysql
  template:
    metadata:
      labels:
        app: poll-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          args: ["--default-authentication-plugin=mysql_native_password"]
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: poll-mysql-secret
                  key: root-password
            - name: MYSQL_DATABASE
              value: fifa_db
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command: ["sh", "-c", "mysqladmin ping -h 127.0.0.1 -uroot -prootpassword"]
            initialDelaySeconds: 20
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi

